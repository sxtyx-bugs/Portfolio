{
  "name": "rest-express",
  "version": "1.0.0",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "dev": "NODE_ENV=development tsx server/index.ts",
    "build:frontend": "npx vite build",
    "build:backend": "npx esbuild server/index.ts --platform=node --packages=external --bundle --format=esm --outdir=dist",
    "build": "npm run build:frontend && npm run build:backend",
    "start": "NODE_ENV=production node dist/index.js",
    "check": "tsc",
    "db:push": "drizzle-kit push"
  },
  "dependencies": {
    "@hookform/resolvers": "^3.10.0",
    "express": "^4.21.2",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "express-session": "^1.18.1",
    "passport": "^0.7.0",
    "passport-local": "^1.0.0",
    "wouter": "^3.3.5",
    "clsx": "^2.1.1",
    "framer-motion": "^11.13.1",
    "zod": "^3.24.2"
    // …keep other dependencies as in your original package.json
  },
  "devDependencies": {
    "typescript": "5.6.3",
    "vite": "^5.4.19",
    "esbuild": "^0.25.0",
    "tsx": "^4.19.1",
    "@vitejs/plugin-react": "^4.3.2"
    // …keep other devDependencies as in your original package.json
  }
}
